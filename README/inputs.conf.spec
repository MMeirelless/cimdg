[cim_synthetic_data://<name>]
model = <value>
* The CIM data model to generate events for.
* Supported models: authentication, network_traffic, web, endpoint, malware, intrusion_detection, dns
* Required.

events_per_interval = <integer>
* Number of events to generate per interval.
* Default: 10
* Maximum: 1000
